import { Alert } from '@mui/material';

const ErrorComponent = () => {
  return <Alert severity="error">An error occured when loading data. Please try again</Alert>;
};

export default ErrorComponent;
